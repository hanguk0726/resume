---
const { project, lang } = Astro.props;
---

<div class="border p-6 rounded-2xl shadow-md space-y-4 bg-white">
  <div>
    <h2 class="text-2xl font-bold text-gray-800">{project.title}</h2>
    <p class="text-gray-600 mt-1">{project.description[lang]}</p>
  </div>

  <div class="text-sm text-gray-500">
    🗓️ {project.date.startYear}.{String(project.date.startMonth).padStart(2, '0')} ~ {project.date.endYear}.{String(project.date.endMonth).padStart(2, '0')}
  </div>

  <div>
    <h3 class="font-semibold text-gray-700">기술 스택</h3>
    <div class="flex flex-wrap gap-2 mt-1">
      {project.techStacks.map((tech: string) => (
        <span class="bg-gray-100 text-gray-800 text-sm px-2 py-1 rounded-full">{tech}</span>
      ))}
    </div>
  </div>

  <div>
    <h3 class="font-semibold text-gray-700">키워드</h3>
    <div class="flex flex-wrap gap-2 mt-1">
      {project.keyword.map((key: string) => (
        <span class="bg-blue-100 text-blue-800 text-sm px-2 py-1 rounded-full">{key}</span>
      ))}
    </div>
  </div>

  {project.links.length > 0 && (
    <div>
      <h3 class="font-semibold text-gray-700">관련 링크</h3>
      <ul class="list-disc list-inside text-blue-600 underline mt-1">
        {project.links.map( (link: string) => (
          <li><a href={link} target="_blank" rel="noopener noreferrer">{link}</a></li>
        ))}
      </ul>
    </div>
  )}

  <div>
    <h3 class="font-semibold text-gray-700">프로젝트 설명</h3>
    <div class="whitespace-pre-wrap text-gray-800 text-sm leading-relaxed mt-1">
      {project.article[lang]}
    </div>
  </div>
</div>
